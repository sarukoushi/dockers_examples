# Bucket for docker (compose) examples

### What is here:
  *  include debian latest (trixie) stable
  *  ssh mount example for using ssh-key during docker build, I use docker compose.

### Feedback on how these could be improved will be welcomed!
