# ${\color{#ffcc00} \text{Bucket for docker examples}}$

### ${\color{#ffcc00} \text{What is here:}}$
  *  my preference of debian latest (trixie) stable
  *  ssh-mount example for using ssh-key during docker build, I use docker compose.

${\color{#ffcc00} \text{Feedback on how these could be improved will be welcomed!}}$
